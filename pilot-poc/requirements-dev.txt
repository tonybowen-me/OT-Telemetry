# Extra dependencies for tests + the standalone DHALSIM lab dataset generator.
-r requirements.txt
pytest==8.3.4
httpx==0.28.1
# Lab only (heavy; NOT needed to run or deploy the web app):
wntr==1.2.0
numpy==1.26.4
